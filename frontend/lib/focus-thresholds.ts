/**
 * Shared UI focus confidence thresholds.
 * Keep these aligned with backend sensitivity tuning to avoid mixed signals.
 */
export const FOCUS_CONFIDENCE_HIGH_THRESHOLD = 0.55;
export const FOCUS_CONFIDENCE_MEDIUM_THRESHOLD = 0.35;
