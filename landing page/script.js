// Simple interactive flair
document.addEventListener('DOMContentLoaded', () => {
    console.log('Misir landing page loaded');
    
    // Add simple animation class to cards on scroll could go here
    // For now, let's just log a message
});
